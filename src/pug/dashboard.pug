extends base.pug

//- This adds extra styles and js to already defined in base.pug
append main_vars
  - css_main.push.apply(css_main, ["css/dashboard.css"])
  - js_main.push.apply(js_main, ["js/dashboard/dash_main.js"])
  
block content
